services:
  - type: web
    name: bycodehub
    runtime: python:3.13
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --worker-class sync --timeout 120 --max-requests 100
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: bycodehub-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: PGSSLMODE
        value: require

databases:
  - name: bycodehub-db
    databaseName: bycodehub
    user: bycodehub
    plan: free
    region: singapore
